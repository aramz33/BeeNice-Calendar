---
tags: [domaine/pro, projet/beeniche, type/handoff, agents]
date: 2026-06-08
---

# Handoff — BeeNice Calendar · Session 2026-06-08

## Ce qui a été fait cette session

### ✅ Fix tests time-dépendants + alignement B3 — commit `fc5edaa`

**Problème racine** : `availability.mjs` utilisait `new Date()` directement. Le week-end, tous les créneaux de la semaine courante tombent dans le passé → `slots[0] = undefined` → cascade de ~34 échecs.

**Fix appliqué :**
- `mvp/server/lib/availability.mjs` — injection `getNow` via `config.now` (changesets de la session précédente, désormais commités)
- `mvp/server/lib/state.mjs` — `createStore(provider, storeConfig = {})` + `now: storeConfig.now`
- 5 fichiers de tests — `TEST_NOW = "2030-01-07T09:00:00.000Z"` ajouté, `withTempStore` passe `{ now: TEST_NOW }` :
  - `availability.test.mjs`
  - `booking-flows.test.mjs`
  - `bookings-validation.test.mjs`
  - `tasks.test.mjs`
  - `state-admin.test.mjs` (2 occurrences : `withTempStore` + `initialStore` direct)

**B3 response shape corrigée :**
- Spec Obsidian attendait `{ id, name, slug, timezone }` — l'implémentation retournait le raw `listPublicBookingLinks()` (`clientName`, `clientId`, `title` inclus)
- `caller-routes.mjs` : mapping `clientName → name`, drop des champs hors spec
- `routes.test.mjs` : test B3 mis en accord avec la shape corrigée

**État final :** 185/185 tests verts

---

## Prochaines tâches backend

| ID | Description | Dépendances |
|----|-------------|-------------|
| **B4** | `PATCH /api/admin/tasks/:taskId` — ajouter `assignedCallerId` dans `admin-routes.mjs` | — |
| **B5** | Invitations calendrier : `prospectEmail` aux participants Nylas + colonne `rsvp_state` sur `bookings` | — |
| **B6** | Google Sheets sync | ⛔ bloqué, attente schéma Julien |

### B4 — détail implémentation

Modifier `mvp/server/lib/http/admin-routes.mjs` :
- `PATCH /api/admin/tasks/:taskId` accepte `{ assignedCallerId?, status? }` dans le body
- Passer `assignedCallerId` à `updateTask()` dans `state.mjs` (vérifie si le champ existe déjà)

---

## Fichiers modifiés ce commit

```
mvp/server/lib/availability.mjs         ← injection getNow
mvp/server/lib/state.mjs                ← createStore(provider, storeConfig)
mvp/server/lib/http/caller-routes.mjs   ← fix response shape B3
mvp/server/lib/http/routes.test.mjs     ← fix test B3
mvp/server/lib/availability.test.mjs    ← TEST_NOW
mvp/server/lib/booking-flows.test.mjs   ← TEST_NOW
mvp/server/lib/bookings-validation.test.mjs ← TEST_NOW
mvp/server/lib/tasks.test.mjs           ← TEST_NOW
mvp/server/lib/state-admin.test.mjs     ← TEST_NOW (×2)
```

---

## Suggested skills pour la prochaine session

```
/obsidian-context          ← toujours en premier
/plan                      ← pour B4 si besoin de planification
/caveman:caveman-commit     ← pour commiter proprement
/sync-project-to-obsidian  ← en fin de session
```

---

## Commandes utiles

```bash
# Tous les tests
node --test mvp/server/lib/**/*.test.mjs mvp/server/lib/*.test.mjs mvp/server/lib/http/*.test.mjs

# Tests ciblés B4
node --test mvp/server/lib/http/routes.test.mjs
```

---

<!-- dernière maj: 2026-06-08 -->
