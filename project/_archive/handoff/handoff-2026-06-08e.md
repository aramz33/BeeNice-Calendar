---
tags: [domaine/pro, projet/beeniche, type/handoff, agents]
date: 2026-06-08
---

# Handoff — BeeNice Calendar · Session 2026-06-08 (session 4)

## Ce qui a été fait cette session

### ✅ F1 — Login page + guards de route — commits `82a8b11` + `c9aa935`

**Contexte :** Aucun écran de login. Toutes les routes accessibles sans session. Backend B2 (better-auth) déjà opérationnel.

**Décisions actées (grillage `/grill-with-docs`) :**
- Route `/` = smart redirect par rôle — ShellPage supprimée
- Session expire après **5h** (sécurité)
- Auto-logout sur 401 dans `apiFetch`
- Pas de `?next=` param — redirect vers role home après login
- `AppChrome` role-aware : callers ne voient pas Admin/Paramètres
- `LoginPage` hors `AppChrome` — page autonome
- Logo BeeNice → home du rôle, bouton "Accueil" supprimé
- Bouton "Déconnexion" dans `AppChrome`

**Fichiers créés / modifiés :**
```
mvp/server/lib/auth.mjs              ← session expiresIn 5h
mvp/src/lib/auth.ts                  ← getSession / signIn / signOut helpers
mvp/src/lib/session.tsx              ← SessionProvider + useSession hook
mvp/src/lib/api.ts                   ← credentials:include + 401 → /login
mvp/src/components/RequireAuth.tsx   ← guard session
mvp/src/components/RequireAdmin.tsx  ← guard admin + role
mvp/src/components/RootRedirect.tsx  ← smart redirect par rôle
mvp/src/pages/LoginPage.tsx          ← formulaire email/password
mvp/src/components/AppChrome.tsx     ← role-aware nav + déconnexion
mvp/src/routes.tsx                   ← guards, /login, RootRedirect
mvp/src/main.tsx                     ← SessionProvider wrapper
mvp/src/pages/ShellPage.tsx          ← SUPPRIMÉ
```

### ✅ Infra tests frontend — Vitest + @testing-library/react

**Première fois** qu'une infra de test frontend est ajoutée au projet.

- `vitest`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `jsdom`
- Config dans `mvp/vite.config.ts` — scoped à `src/**/*.test.{ts,tsx}`
- Setup : `mvp/src/test/setup.ts` + helper `mvp/src/test/router.tsx`
- Script : `npm run test:web`

**21 tests verts** couvrant :
- `auth.ts` helpers (6 tests)
- `RequireAuth` (3 tests)
- `RequireAdmin` (4 tests)
- `RootRedirect` (4 tests)
- `LoginPage` (4 tests)

---

## État final

| Indicateur | Valeur |
|---|---|
| Tests backend | 192 / 192 verts |
| Tests frontend | 21 / 21 verts |
| Build | Propre |
| Session expiry | 5h |

---

## Prochaines tâches frontend

| ID | Description | Dépendances |
|----|-------------|-------------|
| **F2** | Vue colleur unifiée `/caller` | B3 ✅, **F1 ✅** |
| **F3** | Calendrier semaine `@schedule-x/react` | F2 |
| **F4a** | Badge provider Google/Microsoft sur connexions rep | F1 ✅ |
| **F4b** | Dropdown timezone dans formulaire client | F1 ✅ |
| **F4c** | Dropdown assignation tâche depuis vue admin | B4 ✅, F1 ✅ |
| **F5** | Affichage statut RSVP dans détail booking | B5 ✅ |

Backend : B6 (Google Sheets) toujours bloqué — attente schéma Julien.

---

## Commandes utiles

```bash
npm run test:web          # tests frontend (21)
node --test mvp/server/lib/**/*.test.mjs mvp/server/lib/*.test.mjs  # tests backend (192)
npm run dev               # dev server
```

---

## Suggested skills pour la prochaine session

```
/obsidian-context          ← toujours en premier
/grill-with-docs           ← avant F2 (nouveau layout, nouvelles routes)
/plan                      ← pour F2 ou F4c
/caveman:caveman-commit    ← commit
/sync-project-to-obsidian  ← fin de session
```

<!-- dernière maj: 2026-06-08 (session 4) -->



## Sync Codex - 2026-06-08 20:02

Commit pushe : 334f5e2 - feat(caller): split prospect identity fields.

Branche : feat/f2-caller-unified-view -> origin/feat/f2-caller-unified-view.

Changements couverts : formulaire caller avec civilite/prenom/nom separes, persistance backend des champs prospect, exposition API summary, normalisation de la civilite non precisee, test CallerPage mis a jour.

Verifications : backend node --test mvp/server/lib/**/*.test.mjs mvp/server/lib/*.test.mjs mvp/server/lib/http/*.test.mjs = 194 pass ; npm run test:web = 26 pass ; npm run build = OK avec avertissement Vite chunk >500 kB.
