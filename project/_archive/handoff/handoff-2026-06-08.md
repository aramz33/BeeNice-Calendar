---
tags: [domaine/pro, projet/beeniche, type/handoff, agents]
date: 2026-06-08
---

# Handoff — BeeNice Calendar · Session 2026-06-07/08

## Contexte rapide

Backend task B3 livré. Découverte d'un bug de tests time-dépendants (34 échecs chaque week-end). Fix PARTIELLEMENT appliqué — mécanisme d'injection en place, test files pas encore mis à jour.

---

## Ce qui a été fait cette session

### ✅ B3 livré — commit `9493f08`
`feat(api): add GET /api/caller/workspaces`

- Nouveau fichier : `mvp/server/lib/http/caller-routes.mjs`
- `app.mjs` : import + `app.route("/api/caller", createCallerRouter(store))`
- 3 tests ajoutés dans `routes.test.mjs` (401, 200 caller, 200 admin)
- Auth déjà couverte par `requireAuth` sur `/api/caller/*` dans app.mjs

### 🔧 Fix tests time-dépendants — INCOMPLET (non commité)

**Problème** : `availability.mjs` utilise `new Date()` directement. Le week-end, tous les créneaux de la semaine courante sont dans le passé → `slots[0] = undefined` → cascade de 34 échecs.

**Ce qui est fait (unstaged)** :
- `mvp/server/lib/availability.mjs` — injection `getNow` complète :
  - `const getNow = () => (config.now ? new Date(config.now) : new Date())` en tête de `createAvailabilityModule`
  - L.31 : `addMinutes(getNow(), ...)` au lieu de `new Date()`
  - `getRepRollingLoad`, `getRollingCounts`, `assignRep` : signature étendue avec `getNow = () => new Date()`
- `mvp/server/lib/state.mjs` — `createStore(provider, storeConfig = {})` + `now: storeConfig.now` dans la config de `createAvailabilityModule`

**Ce qui RESTE à faire** :
- Mettre à jour les 5 fichiers de tests qui ont leur propre `withTempStore` pour passer `{ now: TEST_NOW }` :
  ```js
  const TEST_NOW = "2030-01-07T09:00:00.000Z"; // lundi fixe
  // dans withTempStore :
  const store = createStore(provider, { now: TEST_NOW });
  ```
  Fichiers concernés :
  1. `mvp/server/lib/availability.test.mjs`
  2. `mvp/server/lib/booking-flows.test.mjs`
  3. `mvp/server/lib/bookings-validation.test.mjs` (withTempStore prend `providerMode`)
  4. `mvp/server/lib/tasks.test.mjs`
  5. `mvp/server/lib/state-admin.test.mjs` (a aussi un `createStore(provider)` direct à la L.136)

**État actuel** : 185/185 tests passent (car c'est un jour de semaine). Sans le TEST_NOW dans les fichiers de tests, ils recasseront le prochain week-end.

---

## Prochaines tâches backend (backlog)

| ID | Description | Dépendances |
|----|-------------|-------------|
| **Fix tests** | Compléter l'injection TEST_NOW dans les 5 fichiers de tests | changes unstaged en place |
| **B4** | `PATCH /api/admin/tasks/:taskId` — ajouter `assignedCallerId` dans `admin-routes.mjs` | — |
| **B5** | Invitations calendrier : `prospectEmail` aux participants Nylas + colonne `rsvp_state` sur `bookings` | — |
| **B6** | Google Sheets sync | ⛔ bloqué, attente schéma Julien |

---

## Fichiers à état non commité (unstaged)

```
mvp/server/lib/availability.mjs   ← injection getNow, 13 lignes modifiées
mvp/server/lib/state.mjs          ← createStore(provider, storeConfig), 3 lignes
```

→ À commiter ensemble avec les test files une fois le TEST_NOW ajouté.

---

## Suggested skills pour la prochaine session

```
/obsidian-context          ← toujours en premier
/plan                      ← si planification nécessaire
/tdd                       ← pour compléter le fix tests (écrire d'abord)
/caveman:caveman-commit     ← pour commiter proprement
/sync-project-to-obsidian  ← en fin de session
```

---

## Commandes utiles

```bash
# Vérifier état des changes
git diff --stat

# Lancer tous les tests
node --test mvp/server/lib/**/*.test.mjs mvp/server/lib/*.test.mjs

# Lancer uniquement les tests de disponibilité
node --test mvp/server/lib/availability.test.mjs
```

---

<!-- dernière maj: 2026-06-08 -->
